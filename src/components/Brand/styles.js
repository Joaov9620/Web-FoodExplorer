import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    flex-direction: row;
    gap: 1rem;

    >img{
        width: 3rem;
        height: 3rem;
    }

    h1>{
        line-height: 2.8rem;
        font-weight: 700;
    }
`;
